# See packages/mqtt-bridge/package-context.yaml for the convention. Porch
# rewrites data.name to the deployment package name on clone; set-namespace
# consumes it so the operator's ServiceAccount and Deployment land in their
# namespace.
apiVersion: v1
kind: ConfigMap
metadata:
  name: kptfile.kpt.dev
  annotations:
    config.kubernetes.io/local-config: "true"
data:
  name: meshtastic-operator
